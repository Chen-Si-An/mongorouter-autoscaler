---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: mongorouterautoscalers.autoscale.mongodb.io
spec:
  group: autoscale.mongodb.io
  names:
    kind: MongoRouterAutoscaler
    listKind: MongoRouterAutoscalerList
    plural: mongorouterautoscalers
    singular: mongorouterautoscaler
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: MongoRouterAutoscaler is the Schema for the CRD
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: MongoRouterAutoscalerSpec defines desired state
            properties:
              policy:
                description: Policy defines scaling policy parameters
                properties:
                  cooldownSeconds:
                    type: integer
                  cpuTargetPercent:
                    type: integer
                  step:
                    type: integer
                  tolerancePercent:
                    type: integer
                  window:
                    type: string
                required:
                - cooldownSeconds
                - cpuTargetPercent
                - step
                - tolerancePercent
                - window
                type: object
              prometheus:
                description: Prometheus defines the Prometheus endpoint
                properties:
                  url:
                    type: string
                required:
                - url
                type: object
              scaleBounds:
                description: ScaleBounds defines the min/max replica limits
                properties:
                  maxReplicas:
                    type: integer
                  minReplicas:
                    type: integer
                required:
                - maxReplicas
                - minReplicas
                type: object
              targetRef:
                description: TargetRef defines the target resource for autoscaling
                properties:
                  name:
                    type: string
                  namespace:
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - policy
            - prometheus
            - scaleBounds
            - targetRef
            type: object
          status:
            description: MongoRouterAutoscalerStatus defines observed state
            properties:
              lastDesiredReplicas:
                format: int32
                type: integer
              lastObservedCPU:
                type: string
              lastScaleTime:
                format: date-time
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
